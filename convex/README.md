# Convex backend

Lance `npm run convex:dev` pour générer `convex/_generated/` et synchroniser le schéma.

Les fichiers `schema.ts` et `products.ts` sont prêts ; les types `_generated` sont créés automatiquement par le CLI Convex.
